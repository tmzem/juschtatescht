# juschtatescht

```rs
fn foo(e: Vec<Result<Foo, Float, Int>>) {
    for x in &vec {
        println!("{x}");
    }
}

// Not really rust wtf

struct Foobar {}

fn stuff<T, U>(thing: Foobar<T, U>, num: Int, rum: i32, f: bool) ref T {
    for ref i in thing {
        print("heya \{i}");
        i += 2
    }
}
```

```rs
class Gewhat {
}
```
